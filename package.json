{
  "name": "code-challenge",
  "version": "1.0.0",
  "description": "website for caMicroscope code challenge",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --port=8080 --host=0.0.0.0 --open=./src"
  },
  "author": "Do Tien Dung",
  "license": "MIT",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
